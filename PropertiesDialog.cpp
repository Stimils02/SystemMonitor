#include "PropertiesDialog.h"

// PropertiesDialog::PropertiesDialog(QWidget *parent)
//     : QDialog(parent), propertiesLabel(new QLabel(this)) {
//     // qDebug() << "PropertiesDialog constructor called";
//     QVBoxLayout *layout = new QVBoxLayout(this);
//     layout->addWidget(propertiesLabel);

//     QPushButton *closeButton = new QPushButton("Close", this);
//     connect(closeButton, &QPushButton::clicked, this, &QDialog::reject);
//     layout->addWidget(closeButton);
// }

// void PropertiesDialog::setProperties(const QString &propertiesText) {
//     propertiesLabel->setText(propertiesText);
// }
